services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Ollama no seu PC: acessível pelo host do Docker
      - OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
